export interface NetworkStats {
  apy: number;
  price: number;
}

export interface Balance {
  amount: string;
  currency: string;
}

export interface Reward {
  amount: string;
  timestamp: string;
  type: string;
} 